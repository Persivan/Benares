name: CI

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  image_scan:
    name: Build Image and Run Image Scan
    runs-on: [ 'Linux', 'X64']

    steps:

    - name: Check sudo
      run: sudo echo "1234"
    
